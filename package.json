{
  "private": true,
  "workspaces": [
    "./apps/*"
  ],
  "scripts": {
    "turbo": "turbo --help",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.11.2"
  }
}